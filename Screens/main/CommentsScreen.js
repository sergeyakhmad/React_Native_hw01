export default function CommentsScreen() {
  return <View style={styles.container}>CommentsScreen</View>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});
